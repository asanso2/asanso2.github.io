include:
  - page-build/site-source-pagesjekyll-ftw
  - _config.yml
plugins:
  - jekyll-redirect-from
  - jekyll-sitemap
defaults:
  -
    scope:
      path:            "**/*.*"
    values:
      sitemap:         true
 

include:
  - page-build/site-source-pagesjekyll-ftw
  - _config.yml
  - {{site.plugins_dir}} 
plugins:
  - jekyll-redirect-from
  - jekyll-sitemap
defaults:
  -
    scope:
      path:            "**/*.*"
    values:
      sitemap:         true
 

source: /page-build/repo
include:
  - page-build/site-source-pagesjekyll-ftw
  - _config.yml
  - page-build/repo
plugins:
  - jekyll-redirect-from
  - jekyll-sitemap
defaults:
  -
    scope:
      path:            "**/*.*"
    values:
      sitemap:         true
 
